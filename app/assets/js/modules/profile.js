
import {carInfo,cars} from "./data.js";
import {openModal,closeModal} from "../core/ui.js";
import {emit} from "../core/events.js";
export function openMyPage(state){openModal("MY PAGE",`<div class="card row"><div class="avatar" style="font-size:70px">${carInfo(state.profile.car).emoji}</div><div><h3>${state.profile.nickname}</h3><div class="muted">${state.profile.plate}</div><div class="muted">${state.profile.status}</div></div></div><div class="card"><label>닉네임</label><input id="pNick" value="${state.profile.nickname}"><label>번호판</label><input id="pPlate" value="${state.profile.plate}"><label>상태</label><input id="pStatus" value="${state.profile.status}"><label>MY CAR</label><select id="pCar">${cars.map(c=>`<option value="${c[0]}" ${c[0]===state.profile.car?"selected":""}>${c[2]} ${c[1]}</option>`).join("")}</select></div>`,[{label:"닫기",onClick:closeModal},{label:"저장",className:"primary",onClick:()=>{state.profile.nickname=document.querySelector("#pNick").value||state.profile.nickname;state.profile.plate=document.querySelector("#pPlate").value||state.profile.plate;state.profile.status=document.querySelector("#pStatus").value;state.profile.car=document.querySelector("#pCar").value;emit("state:save");closeModal()}}])}
export function openUserProfile(user){openModal("차량 프로필",`<div class="card row"><div class="avatar" style="font-size:70px">${carInfo(user.car).emoji}</div><div><h3>${user.nickname}</h3><div class="muted">${user.plate}</div><div class="muted">Lv.${user.level} · ${user.online?"온라인":"오프라인"}</div></div></div>`,[{label:"닫기",onClick:closeModal},{label:"선물하기",onClick:()=>alert("선물 상점을 엽니다.")},{label:"대화",className:"primary",onClick:()=>{closeModal();emit("chat:open",user)}}])}
