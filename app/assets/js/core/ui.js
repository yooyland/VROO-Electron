export function showSystemMessage(message){const el=document.querySelector("#systemMessage");el.textContent=message;el.classList.add("show");clearTimeout(showSystemMessage.timer);showSystemMessage.timer=setTimeout(()=>el.classList.remove("show"),6000)}
export function openModal(title,html,actions=[]){const modal=document.querySelector("#modal");document.querySelector("#modalTitle").textContent=title;document.querySelector("#modalBody").innerHTML=html;const host=document.querySelector("#modalActions");host.innerHTML="";for(const action of actions){const b=document.createElement("button");b.className=action.className||"secondary";b.textContent=action.label;b.onclick=()=>action.onClick?.();host.appendChild(b)}modal.classList.add("show");modal.setAttribute("aria-hidden","false")}
export function closeModal(){const modal=document.querySelector("#modal");modal.classList.remove("show");modal.setAttribute("aria-hidden","true")}
