{
  "name": "vroo-desktop",
  "version": "1.1.0-beta.1",
  "description": "VROO Mobility Lifestyle Platform — User App + role-based Console",
  "main": "main.js",
  "author": "VROO",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "console": "electron . -- --console",
    "dev:platform": "electron . -- --platform",
    "test:heritage-runtime": "electron . -- --heritage-runtime-test",
    "test:workspace-runtime": "electron . -- --workspace-runtime-test",
    "pack": "electron-builder --dir",
    "build:win": "electron-builder --win nsis portable"
  },
  "devDependencies": {
    "electron": "latest",
    "electron-builder": "latest"
  },
  "build": {
    "appId": "com.vroo.desktop",
    "productName": "VROO",
    "asar": true,
    "directories": {
      "output": "dist",
      "buildResources": "assets"
    },
    "files": [
      "main.js",
      "preload.js",
      "app/**/*",
      "console/**/*",
      "shared/**/*",
      "assets/**/*",
      "package.json"
    ],
    "win": {
      "icon": "assets/icon.ico",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "VROO-${version}-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "VROO"
    }
  }
}
